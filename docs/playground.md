# Playground

You can write c programs in the code editor below and run them directly from the browser.

```{ .c .feather }
#include <stdio.h>

int main() {
    printf("Hello, world!");
    return 0;
}
```